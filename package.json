{
  "name": "dalang-erp-root",
  "version": "1.0.0",
  "private": true,
  "description": "DALANG ERP Root Orchestrator",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db:push": "npm run prisma:generate --prefix backend && npm run db:push --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "npx -y concurrently -k -p \"[{name}]\" -n \"Backend,Frontend\" -c \"cyan.bold,green.bold\" \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
